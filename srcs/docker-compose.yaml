services:
  nginx:
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    ports:
      - "443:443"
    volumes:
      - wp_files:/var/www/html
    container_name: nginx
    networks:
      - inception
  mariadb:
    build: 
      context: ./requirements/mariadb
      dockerfile: Dockerfile
    expose:
      - 3306
    env_file:
      - ./.env
    volumes:
      - wp_db:/var/lib/mysql
    container_name: mariadb
    networks:
      - inception
  wordpress:
    build:
      context: ./requirements/wordpress
      dockerfile: Dockerfile
    expose:
      - 9000
    volumes:
      - wp_files:/var/www/html
    container_name: wordpress
    networks:
      - inception

volumes:
  wp_db:
    name: wp_db
    driver: local
    driver_opts:
      o: bind
      type: none
      device: "/Users/hbouhsis/Desktop/database"
  wp_files:
    name: wp_files
    driver: local
    driver_opts:
      o: bind 
      type: none
      device: "/Users/hbouhsis/Desktop/files"

networks:
  inception:
    driver: bridge